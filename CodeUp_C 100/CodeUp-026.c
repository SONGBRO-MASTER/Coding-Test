#include <stdio.h>

int main() {

    unsigned int n;
    scanf("%u", &n);

    printf("%u", n);

    return 0;
}

// 아래도 가능
#include <stdio.h>

int main() {

    int n;
    scanf("%u", &n);

    printf("%u", n);

    return 0;
}